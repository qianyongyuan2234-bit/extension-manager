# 待回看

```dataview
TABLE 主题, 回看日期, 标签
FROM #认知
WHERE 状态 = "待回看" AND 回看日期 <= date(today)
SORT 回看日期 ASC
```

## 近期记录

```dataview
TABLE 主题, 日期, 状态
FROM #认知
SORT 日期 DESC
LIMIT 20
```

## 视角价值统计

```dataview
TABLE 最有价值的视角, 日期
FROM #认知
WHERE 最有价值的视角
SORT 日期 DESC
```
